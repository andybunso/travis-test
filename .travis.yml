sudo: false
language: cpp

addons:
    apt:
        sources:
        - ubuntu-toolchain-r-test
        packages:
        - gcc-4.9-multilib

before_install:
    - chmod u+x ./ghscript.sh
    - find /usr/bin -type f -name "g++*"
    
script:
    - g++ -v

after_success:
    - ./ghscript.sh

sudo: required
language: cpp
before_install:
    - sudo apt-add-repository ppa:ubuntu-sdk-team/ppa -y && sudo apt-get update -q && sudo apt-get install qtchooser qt5-qmake -y
    - export QT_SELECT=qt5
    - sudo updatedb
    - locate boost
script:
    - echo "none"
